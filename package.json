{
  "name": "requires-node-version",
  "version": "1.0.0",
  "description": "Checks to make sure the correct node version is being used and issues an error message if needed.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node",
    "tooling",
    "semver"
  ],
  "author": "Sean O'Donohue",
  "license": "MIT",
  "dependencies": {
    "chalk": "^2.3.1",
    "semver": "^5.5.0"
  }
}
